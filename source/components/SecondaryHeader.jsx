import { StyleSheet, Text, View } from "react-native";

export default function SecondaryHeader({ color, title }) {
    return (
        <View style={styles.container}>
            <Text>Header das telas secundarias</Text>
        </View>
    )
}

const styles = StyleSheet.create({
    container: {

    }
})
